if (document.querySelectorAll('#travel-log .europe')) {
  document.querySelectorAll('#travel-log .europe')
    .forEach((trip) => {
      trip.style.backgroundImage = 'url(../img/trip-europe.jpg)';
    });
}
if (document.querySelectorAll('#travel-log .asia')) {
  document.querySelectorAll('#travel-log .asia')
    .forEach((trip) => {
      trip.style.backgroundImage = 'url(../img/trip-asia.jpg)';
    });
}
if (document.querySelectorAll('#travel-log .americas')) {
  document.querySelectorAll('#travel-log .americas')
    .forEach((trip) => {
      trip.style.backgroundImage = 'url(../img/trip-americas.jpg)';
    });
}
if (document.querySelectorAll('#travel-log .africa')) {
  document.querySelectorAll('#travel-log .africa')
    .forEach((trip) => {
      trip.style.backgroundImage = 'url(../img/trip-africa.jpg)';
    });
}
if (document.querySelectorAll('#travel-log .oceania')) {
  document.querySelectorAll('#travel-log .oceania')
    .forEach((trip) => {
      trip.style.backgroundImage = 'url(../img/trip-oceania.jpg)';
    });
}
